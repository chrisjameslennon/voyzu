# Cross-package contracts

The former application command system has been removed. Use grouped semantic capabilities for operations and read-only master-data contracts for retrieval. Same-package consumers import their own services directly.

See [Contracts](../voyzu-platform-guide/contracts.md) for schemas, registration, composition, transactions and error behavior. Historical command tests are preserved, disabled during migration. This change does not affect Voyzu CLI commands.
