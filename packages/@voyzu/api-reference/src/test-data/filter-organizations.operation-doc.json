{
  "operationId": "filterOrganizations",
  "method": "post",
  "path": "/organization/organizations/filter",
  "summary": "Filter organizations",
  "description": "Filters organization records using the shared filter request body.",
  "requestBody": {
    "required": true,
    "schema": {
      "type": "object",
      "properties": {
        "filters": {
          "type": "array",
          "description": "Filter predicates applied to organization fields.",
          "items": {
            "type": "object",
            "required": ["field", "operator", "value"],
            "properties": {
              "field": {
                "type": "string",
                "description": "Organization field to filter against."
              },
              "operator": {
                "type": "string",
                "enum": ["equals"],
                "description": "Comparison operator applied to the field value."
              },
              "value": {
                "type": "string",
                "description": "Value used by the filter comparison."
              }
            }
          }
        },
        "options": {
          "type": "object",
          "description": "Optional paging, ordering, and result-shaping options."
        },
        "groups": {
          "type": "array",
          "description": "Optional nested filter groups.",
          "items": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["field", "operator", "value"],
              "properties": {
                "field": {
                  "type": "string",
                  "description": "Grouped organization field to filter against."
                },
                "operator": {
                  "type": "string",
                  "enum": ["equals"],
                  "description": "Grouped comparison operator applied to the field value."
                },
                "value": {
                  "type": "string",
                  "description": "Grouped value used by the filter comparison."
                }
              }
            }
          }
        }
      }
    },
    "example": {
      "filters": [
        {
          "field": "status",
          "operator": "equals",
          "value": "ACTIVE"
        }
      ],
      "options": {
        "limit": 20
      }
    }
  },
  "responses": {
    "200": {
      "description": "A filtered list of organizations.",
      "schema": {
        "type": "array",
        "items": {
          "type": "object",
          "required": ["id", "code", "name", "countryCode", "baseCurrencyCode", "status"],
          "properties": {
            "id": {
              "type": "number",
              "description": "Unique numeric identifier for the organization."
            },
            "code": {
              "type": "string",
              "description": "Stable organization code."
            },
            "name": {
              "type": "string",
              "description": "Organization display name."
            },
            "countryCode": {
              "type": "string",
              "description": "Country code used for organization localization."
            },
            "baseCurrencyCode": {
              "type": "string",
              "description": "Base currency code used for organization reporting."
            },
            "status": {
              "type": "string",
              "enum": ["ACTIVE", "INACTIVE"],
              "description": "Current organization status."
            }
          }
        }
      },
      "example": [
        {
          "id": 10001,
          "code": "SAMP-AU",
          "name": "Acme Australia Pty Ltd",
          "status": "ACTIVE"
        }
      ]
    },
    "500": {
      "description": "An unexpected server error occurred.",
      "modelReference": "See Error Response Model"
    }
  }
}
